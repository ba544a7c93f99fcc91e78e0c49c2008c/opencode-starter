{
  "name": "run-tests",
  "description": "Run the project test suite. Auto-detects framework (pytest, jest, vitest, go test, cargo test, dotnet test). Returns output for agent self-correction.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "scope": {
        "type": "string",
        "description": "Advisory hint: path or test name filter. The calling agent should use this to adjust the make test invocation or framework command directly (e.g. pytest path/). Leave empty for full suite."
      }
    },
    "required": []
  },
  "invocation": {
    "type": "shell",
    "command": "make test"
  },
  "output": {
    "type": "text",
    "on_failure": "agent_self_correct"
  },
  "write_gate": false
}
